# impromptu

Manage multiple agents via `tmux`.

- Bring your own TUI (e.g., `gemini-cli`, `claude`).
- Adds status for each agent along with easy navigation.
- Surface and navigate context.
- Shared knowledge base with each agent.